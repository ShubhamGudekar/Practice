#include<stdio.h>
int main()
    {
        int a;
        printf("Enter the Number to Check Odd or Even ");
        scanf("%d",&a);
        if(a%2==0)
        printf("Number is Even");
        else
        printf("Number is Odd");
        return 0;
    }